You are saving a relevant memory to the SQLite sandbox. Inspect the context chain below and decide exactly what text should be stored.

Return ONLY a JSON object with a single "content" field containing that text.

Context chain:
{{ context }}

Example:
{
  "content": "User's birthday is 12 May 1990"
} 